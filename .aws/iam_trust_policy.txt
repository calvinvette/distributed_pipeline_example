This is the standard trust pattern for aws-actions/configure-aws-credentials with claim scoping (repo + branch/tag).  ￼

Example trust policy (tighten sub to your repo/org and optionally to tags) is in iam_trust_policy.json
